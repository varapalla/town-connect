import {GoogleGenAI} from "@google/genai";const key=process.env.GEMINI_API_KEY;if(!key)throw Error("GEMINI_API_KEY is not configured");export const ai=new GoogleGenAI({apiKey:key});export const models=(process.env.GEMINI_MODELS||"gemini-3.8-flash,gemini-3.7-flash,gemini-3.6-flash").split(",").map(x=>x.trim()).filter(Boolean);export async function generate(contents:any){let last:any;for(const model of models){for(let i=0;i<2;i++){try{return await ai.models.generateContent({model,contents});}catch(e:any){last=e;const s=Number(e?.status||e?.code||0);if(![429,500,502,503,504].includes(s))throw e;await new Promise(r=>setTimeout(r,500*(i+1)));}}}throw last;}export function clean(s:string){return s.replace(/^```json\s*/i,"").replace(/^```\s*/i,"").replace(/\s*```$/i,"").trim();}
