export async function getPlaceDetails(placeId:string){const key=process.env.GOOGLE_MAPS_API_KEY;if(!key)throw Error("GOOGLE_MAPS_API_KEY is not configured");const r=await fetch(`https://places.googleapis.com/v1/places/${encodeURIComponent(placeId)}`,{headers:{"X-Goog-Api-Key":key,"X-Goog-FieldMask":"id,displayName,formattedAddress,photos"},cache:"no-store"});if(!r.ok)throw Error(`Google Places failed: ${r.status}`);return r.json();}
